# 表パーツ — セル入力仕様

表パーツの各セルに入力できる内容をまとめる。実装の正は `backend/note_api/app/table_engine.py` と DB 定義（`DB_NOTE_SPEC.md`）を参照。

---

## 1. 共通

| 項目 | 内容 |
|------|------|
| 空セル | 入力なし。表示は空欄 |
| 表示位置 | 左寄せ / 中央寄せ / 右寄せ（セルごとに指定、既定は左寄せ） |
| エラー表示 | `#CYCLE!` 循環参照、`#VALUE!` 型・構文エラー、`#ERROR!` 未対応の入力 |

---

## 2. 文字列型（`string`）

| 項目 | 内容 |
|------|------|
| 入力 | 任意の文字列、または `=` で始まる数式 |
| 数式 | 可（数値型と同じ構文。詳細は [3.2 数式](#32-数式--で始める)） |
| 表示 | 数式でないときは入力文字列をそのまま表示。数式のときは **算出結果** を表示 |
| 数式の結果 | **文字列** でなければならない（数値・日付などは `#VALUE!`） |

**数式の例**

```
=If(Cell(1,1)="apple", "yes", "no")
=Cell(2,1)
="hello"
```

---

## 3. 数値型（`number`）

### 3.1 リテラル

| 入力例 | 説明 |
|--------|------|
| `123` | 整数 |
| `12.5` | 小数 |
| `1,234` | カンマ付き数値（解釈時にカンマは除去） |
| （空） | 0 として扱う |

### 3.2 数式（`=` で始める）

数値型・文字列型・日付型・時刻型・日時型のセルが数式を利用できる。先頭の `=` の後ろが式本体。

#### セル参照

```
Cell(列, 行)
Cell($列, 行)    … 列だけ絶対参照
Cell(列, $行)    … 行だけ絶対参照
Cell($列, $行)   … 列・行とも絶対参照
```

- 列・行は **1 始まり**
- `$` 付き座標は行・列の挿入削除時にずれない
- 参照先が空のときは **0** として扱う
- 参照先が文字列セルで数値に解釈できるときは数値として利用
- 参照先がエラー（`#CYCLE!` 等）のときはそのエラーを伝播

#### 四則演算

| 演算子 | 意味 |
|--------|------|
| `+` `-` | 加減算 |
| `*` `/` | 乗除算（0 除算は `#VALUE!`） |
| `( )` | 括弧 |
| 単項 `-` | 符号反転 |

オペランドは数値リテラル・セル参照・数式の結果（数値に解釈できる値）に限る。

#### 条件分岐 `If`

```
If(条件, 真のときの値, 偽のときの値)
```

- 条件が真なら第 2 引数、偽なら第 3 引数を採用
- 各引数は数値式（四則演算・セル参照・入れ子の `If` など）
- 関数名 `If` は大文字小文字を区別しない（`if` も可）

#### 条件式

条件には比較と論理関数を書ける。

**比較**

| 書式 | 意味 |
|------|------|
| `A=B` | 同値 |
| `A>B` | 左が大きい |
| `A<B` | 左が小さい |

- `A`・`B` は数値リテラル、文字列リテラル（`"..."`）、セル参照、数値式
- **数値**同士は数値として比較
- **文字列**同士は辞書順（コードポイント順）
- **日付・日時・時刻**は時系列で比較（過去 &lt; 未来）
- 数値に解釈できる文字列（例 `"10"`）は数値として比較
- 日付形式の文字列（例 `"2024/06/01"`）は日付として比較
- 型が一致しない比較（例 数値と非数値文字列）は `#VALUE!`

**論理関数**

| 書式 | 意味 |
|------|------|
| `And(A, B, C, …)` | すべて真のとき真 |
| `Or(A, B, C, …)` | いずれかが真のとき真 |
| `Not(A)` | 条件の否定 |

- `A`・`B` 等は比較式、または入れ子の `And` / `Or` / `Not`
- 関数名は大文字小文字を区別しない

**条件式の例**

```
If(Cell(1,1)>10, 100, 0)
If(Cell(1,1)=Cell(2,1), 1, 0)
If(And(Cell(1,1)>5, Cell(1,1)<10), 1, 0)
If(Or(Cell(1,1)<0, Cell(1,1)>100), 9, 0)
If(Not(Cell(1,1)=0), Cell(1,1), 0)
If(Cell(1,1)>"apple", 1, 0)
If(Cell(1,1)<Cell(2,1), 1, 0)    … 日付セル同士の比較
```

#### 数式の結果

数値型セルの数式の最終結果は **数値** でなければならない。数値に解釈できない文字列が選ばれた場合は `#VALUE!`。

#### 数式の例

```
=1+2*3
=Cell(1,1)+Cell(2,1)
=If(Cell(1,1)>0, Cell(1,1)*2, 0)
=If(And(Cell(1,1)>5, Cell(2,1)<10), Cell(3,1), 0)+1
```

### 3.3 表示形式

| 表示形式 | 例（値 1234.5） |
|----------|-----------------|
| 整数 | `1235` |
| 小数2桁 | `1234.50` |
| カンマ付き整数 | `1,235` |
| カンマ付き小数2桁 | `1,234.50` |

---

## 4. 日付型（`date`）

| 項目 | 内容 |
|------|------|
| 入力形式 | `YYYY/MM/DD` または `MM/DD`（`MM/DD` は当年として解釈） |
| ハイフン | `YYYY-MM-DD` も可（内部で `/` に正規化） |
| 数式 | 可。結果は **日付** でなければならない（不一致は `#VALUE!`） |

| 表示形式 | 例 |
|----------|-----|
| `YYYY/MM/DD` | `2024/06/15` |
| `MM/DD` | `06/15` |

---

## 5. 時刻型（`time`）

| 項目 | 内容 |
|------|------|
| 入力形式 | `hh:mm` または `hh:mm:ss`（24 時間制）。時は 1 桁でも可（例 `1:00`） |
| 数式 | 可（数値型・文字列型と同じ構文。詳細は [3.2 数式](#32-数式--で始める)） |
| 表示 | 数式でないときは入力を時刻として表示。数式のときは **算出結果** を表示 |
| 数式の結果 | **時刻** でなければならない（数値・文字列などは `#VALUE!`） |

**数式の例**

```
=Cell(1,1)
=If(Cell(1,1)<Cell(2,1), Cell(1,1), Cell(2,1))
```

| 表示形式 | 例 |
|----------|-----|
| `hh:mm` | `09:30` |
| `hh:mm:ss` | `09:30:00` |

---

## 6. 日時型（`datetime`）

| 項目 | 内容 |
|------|------|
| 入力形式 | `YYYY/MM/DD hh:mm` または `YYYY/MM/DD hh:mm:ss` |
| 数式 | 可。結果は **日時** でなければならない（不一致は `#VALUE!`） |

| 表示形式 | 例 |
|----------|-----|
| `YYYY/MM/DD hh:mm` | `2024/06/15 09:30` |
| `YYYY/MM/DD hh:mm:ss` | `2024/06/15 09:30:00` |

日付型・日時型の値は、数値セルの条件式でセル参照したとき **時系列として比較** できる。

---

## 7. 表の管理項目

| 項目 | 説明 |
|------|------|
| タイトル | 表全体のタイトル。パーツ表示時に表の上へ表示（空のときは非表示） |
| 行数・列数 | 編集画面で行・列の追加・削除により変更 |

---

## 8. 関連ドキュメント

| ファイル | 内容 |
|----------|------|
| `DB_NOTE_SPEC.md` | DB スキーマ |
| `API_NOTE_SPEC.md` | 表 API（E 系） |
